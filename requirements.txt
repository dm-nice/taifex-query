 requirements.txt（進階版
🧩 套件用途說明
• 	requests → 呼叫 TAIFEX API，取得 JSON 資料
• 	pandas → 資料表處理、CSV 儲存、分析
• 	matplotlib → 基礎折線圖、趨勢圖
• 	numpy → 數值運算、陣列處理，搭配 pandas 更快
• 	seaborn → 進階統計視覺化（漂亮的折線圖、熱力圖）
• 	schedule → 簡單排程工具，可設定每天早上自動執行程式
• 	python-dotenv → 管理環境變數（例如 API Key、設定檔），避免寫死在程式裡

🚀 使用方式
1. 	在專案根目錄建立 
2. 	安裝套件：pip install -r requirements.txt
3. 	更新套件清單（當你新增其他套件時）：pip freeze > requirements.txt




requests==2.31.0
pandas==2.1.1
matplotlib==3.8.0

🧩 套件用途說明
• 	requests → 呼叫 TAIFEX API，取得 JSON 資料
• 	pandas → 處理資料表、轉換 CSV、做分析
• 	matplotlib → 繪製折線圖、趨勢圖












🚀 使用方式
1. 	在專案根目錄建立 requirements.txt
2. 	安裝套件：
3. 	更新套件清單（當你新增其他套件時）：pip freeze > requirements.txt


