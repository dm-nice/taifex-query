# 共同開發規範書

**版本**: 4.0
**最後更新**: 2025-12-05
**適用範圍**: 所有 fetcher 模組

---

## 📋 目錄

1. [快速參考](#快速參考-quick-reference) ⭐
2. [概述](#概述)
3. [核心規範](#核心規範)
4. [介面定義](#介面定義)
5. [資料格式標準](#資料格式標準)
6. [錯誤處理規範](#錯誤處理規範)
7. [開發環境要求](#開發環境要求)
8. [測試規範](#測試規範)
9. [交付檢查清單](#交付檢查清單)
10. [常見問題 FAQ](#常見問題-faq)

---

## 快速參考 (Quick Reference)

> 💡 **給趕時間的開發者**：這裡是最核心的規範，10分鐘快速上手！
>
> 詳細說明請往下閱讀各章節。

### 🎯 統一文字格式規範

#### ✅ 成功時格式（v4.0 與 v5.0）
```
[ YYYY.MM.DD  FXX{描述}   source: {來源} ]  # v4.0（舊格式）
FXX: {描述} [source]             # v5.0（新格式，可省略括號與前置日期）
```

**範例**:
```
[ 2025.12.03  F01台指期外資淨額 -29,439 口（多方 19,214，空方 48,653）   source: TAIFEX ]  # v4.0
F11: 台灣股票大盤當日收盤價 : 28,400.73 [mis.twse.com.tw]  # v5.0
```

**格式元素**:
- `[ ... ]` - 中括號 + 空格
- `YYYY.MM.DD` - 日期格式（點分隔）
- `FXX` - 模組代號（大寫字母 + 數字）
- `{描述}` - 模組特定的中文描述 + 數據
- `{數值}` - 使用千分位逗號（如 -29,439）
- `source: {來源}` - 固定格式標記資料來源

#### ❌ 錯誤時格式
```
[ YYYY.MM.DD  FXX 錯誤: {錯誤訊息}   source: {來源} ]
```

**範例**:
```
[ 2025.11.30  F01 錯誤: 該日無交易資料（可能是假日或休市日）   source: TAIFEX ]
```

**規則**:
- 錯誤標記: `{模組代號} 錯誤: {錯誤訊息}`
- 不區分 failed/error（統一為錯誤格式）
- 錯誤訊息使用中文，清楚描述問題

### 🔧 必須實作的函式

```python
def fetch(date: str) -> str:
    """
    抓取指定日期的資料

    Args:
        date: 查詢日期，格式 YYYY-MM-DD

    Returns:
        統一格式的文字字串
    """
```

### 📝 格式化函式範本

```python
from typing import Dict, Optional

def format_fxx_output(date: str, status: str, data: Optional[Dict] = None, error: Optional[str] = None) -> str:
    """格式化輸出為統一文字格式"""
    date_formatted = date.replace("-", ".")  # YYYY-MM-DD → YYYY.MM.DD

    if status == "success" and data:
        # 根據模組特性格式化成功輸出
        value = data.get("some_value", 0)
        return f"[ {date_formatted}  FXX{描述} {value:,}   source: {來源} ]"
    else:
        error_msg = error or "未知錯誤"
        return f"[ {date_formatted}  FXX 錯誤: {error_msg}   source: {來源} ]"
```

### ✅ 四大規範

1. ✅ **回傳類型**：必須是 `str`（統一文字格式）
2. ✅ **錯誤處理**：所有錯誤都轉換為文字格式，不拋出例外
3. ✅ **日期格式**：輸入 `YYYY-MM-DD` → 輸出 `YYYY.MM.DD`
4. ✅ **模組代號**：大寫（F01, F02...）

---

## 概述

### 專案架構

```
C:\Taifex\
├── run.py              # 主控程式（不需修改）
├── modules\            # 正式模組目錄
│   └── xxx_fetcher.py
├── dev\                # 開發/驗收目錄
│   └── xxx_fetcher_dev.py
└── data\               # 輸出目錄（自動產生）
    ├── YYYY-MM-DD_xxx_fetcher.json
    └── YYYY-MM-DD_run.log
```

### 工作流程

```
開發階段              驗收階段              正式階段
   ↓                     ↓                     ↓
寫在 dev/         run.py 測試           移至 modules/
xxx_dev.py       產生 JSON             改名 xxx.py
   ↓                     ↓                     ↓
獨立測試           檢查格式              自動執行
可執行             檢查結果              每日排程
```

### 📁 run.py 執行與 data/ 輸出

- 使用 `run.py [日期] [模式] [--module 模組名稱]` 透過統一入口執行模組。
- `data/` 由 `run.py` 自動建立，輸出檔名格式為 `YYYY-MM-DD_HHMM_模組名稱[_dev].txt` 承載 v4/v5 文字輸出。
- 同一支模組在正式/驗收模式會產生不同檔名（末尾 `_dev`），日誌 (.log) 與輸出檔一併保留紀錄。
- 若需要調整輸出格式，只要讓 `fetch()` 回傳符合格式的字串，log 與 `data/` 會自動同步。


---

## 核心規範

### 1. 混合模式設計原則 ⭐⭐⭐

**這是最重要的規範！**

#### 程式內部：使用英文 key
```python
# ✅ 正確
return {
    "module": "f01",
    "date": "2025-12-01",
    "status": "success",
    "data": {...}
}

# ❌ 錯誤
return {
    "模組": "f01",      # 不要用中文 key
    "日期": "2025-12-01",
    "狀態": "成功"
}
```

#### 使用者看到的：使用中文訊息
```python
# ✅ 正確：訊息內容用中文
"summary": "台指期外資淨額 -29,032 口（多方 18,268，空方 47,300）"
"error": "該日無交易資料（可能是假日或休市日）"

# ❌ 錯誤：訊息用英文
"summary": "Foreign net position: -29,032"
"error": "No trading data available"
```

#### 為什麼這樣設計？

| 面向 | 語言 | 原因 |
|------|------|------|
| 程式內部（key） | 英文 | 符合國際標準、避免編碼問題、易於維護 |
| 使用者介面（訊息） | 中文 | 友善易懂、本地化體驗 |

---

## 介面定義

### 必須實作的函式

```python
def fetch(date: str) -> str:
    """
    抓取指定日期的資料

    Args:
        date: 查詢日期，格式 YYYY-MM-DD

    Returns:
        統一格式的文字字串
        格式: [ YYYY.MM.DD  FXX{描述}   source: {來源} ]
    """
```

### 函式規範

- ✅ **函式名稱**：必須是 `fetch`
- ✅ **參數**：只接受一個參數 `date: str` (YYYY-MM-DD)
- ✅ **回傳**：必須是 `str`（統一格式文字）
- ✅ **不拋出例外**：所有錯誤都用錯誤格式文字表示

---

## 資料格式標準

### 統一文字格式定義

所有模組必須回傳統一格式的文字字串，格式如下：

#### ✅ 成功時

```
[ 2025.12.03  F01台指期外資淨額 -29,439 口（多方 19,214，空方 48,653）   source: TAIFEX ]
```

**格式規範**：
- 外框: `[ ... ]` (中括號 + 空格)
- 日期: `YYYY.MM.DD` (點分隔)
- 模組代號: 大寫字母 + 數字 (如 F01, F02)
- 描述: 模組特定的中文描述 + 數據
- 來源: 固定格式 `source: {來源名稱}`

#### ❌ 失敗/錯誤時

```
[ 2025.11.30  F01 錯誤: 該日無交易資料（可能是假日或休市日）   source: TAIFEX ]
```

**格式規範**：
- 錯誤標記: `{模組代號} 錯誤: {錯誤訊息}`
- 不再區分 failed/error (統一為錯誤格式)
- 錯誤訊息使用中文，清楚描述問題

### 日期格式轉換

```python
date = "2025-12-03"  # 輸入
date_formatted = date.replace("-", ".")  # 轉換
# 輸出: "2025.12.03"
```

### 模組代號提取

從模組名稱提取代號：

```python
import re

def extract_module_id(module_name: str) -> str:
    """提取模組代號"""
    match = re.match(r'([a-z]\d+)', module_name, re.IGNORECASE)
    if match:
        return match.group(1).upper()
    return "FXX"
```

---

### 🔎 數據來源約定

- 每個模組需在注釋或 docstring 明確註記主要資料來源（網站 / API）。
- 若使用即時 API（如 `mis.twse.com.tw` 提供台灣加權指數），需補註：
    - 資料延遲（例：收盤需等官方下午 3:30 之後才能穩定回傳）。
    - 備援策略（API 無資料或被限制時，使用舊版 HTML 抓取或延遲重試）。
- 若來源為 TAIFEX 官方頁面，須記錄 key 欄位名稱與備援欄位列表，以檢查欄位名稱漂移。
- 所有來源需被記錄於 `dev/團隊開發爬蟲的避雷指南.md`，方便後續追蹤與排查。

## 錯誤處理規範

### 狀態碼使用時機

| 狀態碼 | 使用時機 | 範例 |
|--------|---------|------|
| success | 成功取得資料 | 正常交易日，資料完整 |
| failed | 業務邏輯失敗 | 假日無資料、找不到欄位、欄位格式異常 |
| error | 技術性錯誤 | 網路逾時、HTTP 錯誤、日期格式錯誤 |

### 必須處理的錯誤情況

#### 1. 網路相關錯誤 → error

```python
try:
    response = requests.get(url, timeout=30)
    response.raise_for_status()
except requests.Timeout:
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "error",
        "error": "連線逾時，請檢查網路連線"
    }
except requests.HTTPError as e:
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "error",
        "error": f"HTTP 錯誤 {e.response.status_code}"
    }
except requests.RequestException as e:
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "error",
        "error": f"網路請求失敗: {str(e)}"
    }
```

#### 2. 資料格式錯誤 → failed

```python
# 找不到表格
if len(tables) == 0:
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "failed",
        "error": "該日無交易資料（可能是假日或休市日）"
    }

# 找不到目標欄位
if target_col is None:
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "failed",
        "error": f"找不到目標欄位，可用欄位: {df.columns.tolist()}"
    }
```

#### 3. 參數錯誤 → error

```python
try:
    datetime.strptime(date, "%Y-%m-%d")
except ValueError:
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "error",
        "error": "日期格式錯誤，請使用 YYYY-MM-DD"
    }
```

#### 4. 未預期的錯誤 → error

```python
except Exception as e:
    logger.exception("未預期的錯誤")
    return {
        "module": MODULE_ID,
        "date": date,
        "status": "error",
        "error": f"未預期的錯誤: {str(e)}"
    }
```

### 錯誤訊息撰寫原則

✅ **好的錯誤訊息**：
- "找不到身份別欄位，可用欄位: ['商品', '交易人', '多方']"
- "該日無交易資料（可能是假日或休市日）"
- "連線逾時，請檢查網路連線"

❌ **不好的錯誤訊息**：
- "錯誤" （太籠統）
- "Error: KeyError" （英文 + 技術術語）
- "list index out of range" （Python 原始錯誤）

---

## 開發環境要求

### Python 版本
- **最低要求**: Python 3.9+
- **建議版本**: Python 3.11+

### 必要套件

```bash
pip install requests pandas lxml
```

| 套件 | 用途 | 版本要求 |
|------|------|---------|
| requests | HTTP 請求 | >= 2.28.0 |
| pandas | 資料處理 | >= 2.0.0 |
| lxml | HTML 解析 | >= 4.9.0 |

### 模組結構範本

```python
"""
模組說明文字
"""

import sys
import json
import logging
import requests
import pandas as pd
from datetime import datetime
from typing import Dict, Optional

# 模組識別
MODULE_ID = "f01"  # 對應 JSON 的 "module" 欄位
MODULE_NAME = "f01_fetcher"

# 設定 logging
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s'
)
logger = logging.getLogger(__name__)


def fetch(date: str) -> dict:
    """主要函式"""
    # 你的實作...
    pass


def main():
    """獨立測試用"""
    if len(sys.argv) > 1:
        test_date = sys.argv[1]
    else:
        test_date = '2025-11-28'
    
    result = fetch(test_date)
    print(json.dumps(result, ensure_ascii=False, indent=2))
    
    sys.exit(0 if result.get("status") == "success" else 1)


if __name__ == '__main__':
    main()
```

---

## 測試規範

### 獨立測試

你的程式必須能夠獨立執行：

```bash
# 使用預設日期
python f01_fetcher_dev.py

# 指定日期
python f01_fetcher_dev.py 2025-12-01
```

**輸出要求**：
- ✅ 印出 JSON 格式的結果
- ✅ 根據狀態設定退出碼（0=成功, 1=失敗）

### 整合測試

透過 run.py 測試：

```bash
# 驗收模式
python run.py 2025-12-01 dev --module f01_fetcher_dev

# 檢查輸出
type data\2025-12-04_f01_fetcher_dev_dev.json
```

### 測試案例要求

**每個模組至少需要 3 個測試案例**：

1. **正常交易日** → 預期 `status: "success"`
2. **假日/休市日** → 預期 `status: "failed"`
3. **邊界情況** → 根據模組特性決定

範例：
```python
# 測試案例寫在規範書中
| 測試日期 | 預期狀態 | 預期資料 | 備註 |
|---------|---------|---------|------|
| 2025-12-02 | success | 多方: 18808, 空方: 48032 | 正常交易日 |
| 2025-11-30 | failed | - | 週六無交易 |
| 2025-12-25 | failed | - | 聖誕節休市 |
```

---

## 交付檢查清單

### 檔案交付

- [ ] 檔案名稱正確：`xxx_fetcher_dev.py`
- [ ] 放在正確位置：`dev/` 目錄
- [ ] 包含 `fetch()` 函式
- [ ] 包含 `main()` 函式（供獨立測試）

### 格式檢查

- [ ] 回傳格式為 `dict`
- [ ] 包含所有必要欄位
- [ ] `status` 值正確（"success" / "failed" / "error"）
- [ ] 程式內部使用英文 key
- [ ] 顯示訊息使用中文

### 功能檢查

- [ ] 能正確抓取目標資料
- [ ] 能處理假日/休市日
- [ ] 能處理網路錯誤
- [ ] 能處理資料格式異常
- [ ] 錯誤訊息清楚明確

### 程式碼品質

- [ ] 有適當的註解和 docstring
- [ ] 有完整的錯誤處理（try-except）
- [ ] 不會直接拋出未處理的 Exception
- [ ] 能獨立執行測試
- [ ] 程式碼結構清晰

### 驗收測試

```bash
# 1. 獨立測試
python dev/xxx_fetcher_dev.py 2025-12-01

# 2. 整合測試
python run.py 2025-12-01 dev --module xxx_fetcher_dev

# 3. 檢查輸出
type data\2025-12-04_xxx_fetcher_dev_dev.json
```

**通過標準**：
- ✅ JSON 格式正確
- ✅ 必要欄位完整
- ✅ 資料值不是 "-" 或空
- ✅ 錯誤情況有清楚訊息

---

## 常見問題 FAQ

### Q1: 為什麼要用英文 key 但中文訊息？

**A**: 這是混合模式設計：
- **英文 key**: 符合程式設計慣例，避免編碼問題，便於國際化
- **中文訊息**: 使用者友善，訊息清楚易懂

範例：
```json
{
  "status": "success",              // ← 程式用英文 key
  "summary": "台指期外資淨額 -29,032 口"  // ← 使用者看中文訊息
}
```

### Q2: data 裡面的 key 要用英文還是中文？

**A**: **必須用英文**！

```json
{
  "data": {
    "long_position": 18268,    // ✅ 正確
    "short_position": 47300    // ✅ 正確
  }
}

// ❌ 錯誤
{
  "data": {
    "多方口數": 18268,
    "空方口數": 47300
  }
}
```

但在 `summary` 可以用中文：
```json
{
  "summary": "多方口數 18,268，空方口數 47,300"  // ✅ 這裡可以用中文
}
```

### Q3: status 應該用 "success" 還是 "成功"？

**A**: **必須用 "success"**！

```python
# ✅ 正確
"status": "success"
"status": "failed"
"status": "error"

# ❌ 錯誤
"status": "成功"
"status": "失敗"
"status": "錯誤"
```

### Q4: 假日無資料應該回傳 failed 還是 error？

**A**: **回傳 failed**

- `failed`: 業務邏輯失敗（假日本來就沒資料，這是正常的）
- `error`: 技術性錯誤（例如網路斷線，這是異常的）

```python
# ✅ 正確
if len(tables) == 0:
    return {
        "status": "failed",
        "error": "該日無交易資料（可能是假日或休市日）"
    }
```

### Q5: 錯誤訊息要寫多詳細？

**A**: 盡量詳細，但要用中文！

```python
# ✅ 很好：詳細且有幫助
"error": "找不到身份別欄位，可用欄位: ['商品', '交易人', '多方', '空方']"

# ⚠️ 普通：有基本資訊
"error": "找不到身份別欄位"

# ❌ 不好：太籠統
"error": "資料錯誤"

# ❌ 很糟：英文 + 技術術語
"error": "KeyError: '身份別'"
```

### Q6: 需要處理編碼問題嗎？

**A**: 通常不需要，但建議設定：

```python
response = requests.get(url, timeout=30)
response.encoding = "utf-8"  # ← 明確指定 UTF-8
```

### Q7: 需要寫 docstring 嗎？

**A**: **必須！** 至少要有 `fetch()` 函式的 docstring：

```python
def fetch(date: str) -> dict:
    """
    抓取指定日期的台指期貨外資未平倉資料
    
    Args:
        date: 查詢日期，格式 YYYY-MM-DD
        
    Returns:
        結果字典，包含 status, data 等欄位
    """
```

### Q8: 可以加入自己的輔助函式嗎？

**A**: **可以！** 鼓勵將複雜邏輯拆分成小函式：

```python
def convert_to_int(value) -> int:
    """輔助函式：轉換為整數"""
    if pd.isna(value):
        return 0
    return int(str(value).replace(',', '').strip())

def find_target_column(df, keywords):
    """輔助函式：尋找目標欄位"""
    # ...
```

### Q9: 要怎麼測試我的程式？

**A**: 三步驟測試法：

```bash
# 1. 獨立測試（快速驗證）
python xxx_fetcher_dev.py 2025-12-01

# 2. 整合測試（完整流程）
python run.py 2025-12-01 dev --module xxx_fetcher_dev

# 3. 檢查輸出（確認結果）
type data\2025-12-04_xxx_fetcher_dev_dev.json
```

### Q10: 我的程式通過驗收後呢？

**A**: 驗收通過後的流程：

1. 將檔案移至 `modules/` 目錄
2. 重新命名（移除 `_dev`）：`xxx_fetcher.py`
3. 納入正式執行流程
4. 由 `run.py` 自動呼叫

```bash
# 移動檔案
move dev\xxx_fetcher_dev.py modules\xxx_fetcher.py

# 正式執行
python run.py 2025-12-01
```

---

## 附錄：完整範例

### 最小可用範例

```python

## 🧪 Debug 腳本管理

- 若有新增 `debug_*.py`、`debug_mis` 等輔助腳本，請務必集中放置於專案根目錄的 `debug/`，避免散落造成維護困難。
- 在檔案內註明對應模組/用途與執行命令（例如 `python debug/debug_f11.py`），方便日後追蹤與重複驗證。
- 任何與 debug 腳本相關的 log、輸出檔案也建議一併整理至 `debug/output/` 或類似子目錄，保持資料可追溯。

"""
最小可用的 fetcher 範例
"""

import sys
import json
import requests
import pandas as pd
from datetime import datetime

MODULE_ID = "example"

def fetch(date: str) -> dict:
    """抓取資料"""
    try:
        # 驗證日期
        datetime.strptime(date, "%Y-%m-%d")
        
        # 發送請求
        url = "https://example.com/api"
        response = requests.get(url, timeout=30)
        response.raise_for_status()
        
        # 解析資料
        data = response.json()
        
        # 回傳成功
        return {
            "module": MODULE_ID,
            "date": date,
            "status": "success",
            "summary": "成功取得資料",
            "data": {
                "value": data["value"]
            },
            "source": "EXAMPLE"
        }
        
    except requests.Timeout:
        return {
            "module": MODULE_ID,
            "date": date,
            "status": "error",
            "error": "連線逾時"
        }
    except Exception as e:
        return {
            "module": MODULE_ID,
            "date": date,
            "status": "error",
            "error": f"未預期的錯誤: {str(e)}"
        }

def main():
    """測試用"""
    test_date = sys.argv[1] if len(sys.argv) > 1 else '2025-12-01'
    result = fetch(test_date)
    print(json.dumps(result, ensure_ascii=False, indent=2))
    sys.exit(0 if result.get("status") == "success" else 1)

if __name__ == '__main__':
    main()
```

---

**文件結束**

有任何問題請參考個別模組的開發規範書，或聯繫專案負責人。